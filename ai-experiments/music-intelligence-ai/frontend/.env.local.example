# Copy to .env.local (Next.js loads this automatically; do not commit secrets).
#
# Backend API base URL (no trailing slash). Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
